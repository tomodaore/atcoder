/*
完敗
*/

#include <iostream>
#include <vector>
using namespace std;
using ll = long long;

int main(){
    ll n,q,c,p;
    cin >> n >> q;
    vector<ll> a[n];
    for(int i = 0;i < n;i++){
        a[i].push_back(i);
    }
    for(ll i = 0;i < q;i++){
        cin >> c >> p;

    }

}